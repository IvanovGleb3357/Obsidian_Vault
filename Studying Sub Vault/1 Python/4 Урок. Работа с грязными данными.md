		[[Курс Аналитик Данных karpov.course]]
## Проверка на начало строки .startswith()
	'Abyss'.startswith('Ab')
	True

## Конвертация типов в датафрейме .astype()
	1. df = df.astype({'money': 'float'})  # df.money will be rational number after this line

	2. df.height = df.height.astype('float')  # df.height will be rational number

## .drop() или удаление колонок из датафрейма
Можно как columns:
	df = df.drop(columns='Date')  # drop Date column
Так и rows:
	df = df.drop(index=350)  # drop row with 350 index

## Удаление дубликатов из датафрейма .drop_duplicates()
Удаление:
	df = df.drop_duplicates()  # df will contain <= rows than before after this operation
Вывести все дубликаты:
	df.loc[df.duplicated()]
Если нужны определнные колонки то аргумент subset:
	 Drop only if duplicates are in 'Date' or 'Last' columns or both of them
	df.drop_duplicates(subset=['Date', 'Last'])

## Соединение сравнений:
	1 < 2 < 3
	True

## Конвертация времени pd.to_datetime() и parse_dates
	meal_data['Date'] = pd.to_datetime(meal_data['Date'])
Также есть атрибут **dt**, который в связке с:
	-dt.year
	-dt.month
Позволит доставть год или месяц соответственно.
								![[Pasted image 20230725195033.png]]

А в **parse_dates** необзодимо передавать колонки, которые нужно прочитать как дату:
	pd.read_csv('some.csv', parse_dates=[1])  # order of columns starting from 0

## Открывание файлов через open()
Сперва:
	file = open('path_to_file')
Затем у file есть много методов на чтение содержимого, например:
	lines = file.readlines()  # lines is a list with lines from the file
Вконце нужно закрыть файл:
	lines = file.readlines()  # lines is a list with lines from the file

## Просмотр содержимого папок через os

	import os
	os.listdir('/papka') - просмотр содержимого в папке

## Удаление пропущеных значений .dropna()

	df.dropna()

## Объединение датафреймов и добавление строк

1. Для объединения двух или более датафрэймов существует метод `pd.concat()`. Он принимает кортеж из нескольких датафрэймов и возвращает один большой из них. Соединять можно вертикально (увеличиваем число строк) или горизонтально (увеличиваем число столбцов).

2. Есть менее общий вариант — метод `append()` принимает другой датафрэйм и прибавляет его вертикально. Для записывания в датафрэйм новых строк из другого датафрэйма можно использовать метод `append`, возвращающий новый датафрэйм, где прибавлены строки из второго.
