[[Курс Аналитик Данных karpov.course]]

## Замена элементов в зависимости от их значений
Функция `np.where()` позволяет задать новые значения, основываясь на старых. Она принимает 3 аргумента:

- `condition` — условие, то есть серия со списком `True` и `False`. 
- `x` — на что заменить `True`. 
- `y` — на что заменить `False`

	![](https://lh3.googleusercontent.com/dF39_saMNSgwKkYgCbSG__uWoIsJ8ow2JVcb4grnOp9pW4iLu_ILjZkNhZhlVwzc3Yw6ovkhFvxZdjErTOsW1Il6ckPEpc1y5cRVqm71zPP6FS_hoph8kd6QPoVn3hpXm3yDPSet)

## Проверка на пропущеные значения

`` `notna` `` — это метод-антоним `` `isna` ``, возвращает True, если значение не NA. Альтернативный способ получить такой результат — инвертировать результат применения `` `isna` `` с помощью `` `~` `` (так во многих языках обозначают not, в логических сериях pandas также).
Также:

	df.notna()
	# Same as previous
	~df.isna()

## Работа с ошибками

	expenditures = 0
	income = 100

	try:
	    ratio = income / expenditures
	except:
	    print('Something went wrong, mb expenditures are 0?')

## Булиновские результаты логических серий

	1. .all()
Проверяет, все ли значен я в серии True, если нет - то False

	2. .any()
Проверяет, если ли хоть одно значение True в серии, если да - то True

## Рандомное значение:

	# returns number from [a, b]
	random.randint(a, b)

## API и Токен
**Токен** — это уникальная последовательность символов, позволяющая авторизоваться на сайте и работать с API.

## json
Библиотека для работы с json'ом — одним из самых распространённых форматов данных в вэбе. Он используется для пересылки данных между веб-сервисами, и очень похож на словари в python. Одноимённая библиотека `` `json` `` используется для его преобразования в действительно питоновский словарь.

	import json

	# Convert json to python dict
	json_data = json.loads(some_json)

## Интернет-запросы
Библиотека `` `requests` `` позволяет взаимодействовать с сайтами. Метод `` `get()` `` принимает ссылку на сайт в виде строки, и возвращает объект, содержащий ответ с сайта в виде строки:

	import requests
	query = requests.get(url)