## Ускоряем пандас
## pd.iterrows()
pd.iterrows() - возвращает строку датафрейма в виде пар (index, series)

**Пример**

	for index, row in df[:3].iterrows():
	    print("Индекс {}; Число пассажиров: {}".format(index, row['passenger_count']))

## pd.itertuples()
На каждой итерации возвращает именованный кортеж (named tuple) для каждой строки датафрейма. Параметр `` `index` `` по умолчанию равен True, поэтому первым элементом кортежа следует индекс, далее — содержимое строки.

	for row in df[:3].itertuples():
	    print("Индекс {}; Число пассажиров: {}".format(row.Index, row.passenger_count))


**Самое быстрое - написать функцию и вызвать ее.**

# Shape of data или работа с таблицами

## stack
Помещает уровень столбцов в уровни индекса строк. Реузльтирующий объект - Series.
			![[Pasted image 20230808212808.png]]

Например:

					![[Pasted image 20230808212911.png]]

Полученным индексам также можно присвоить названия:

	df_stacked.index.names = ['id', 'column']
	df_stacked

**stack() принимает 2 аргумента: 
1. level=-1. Актуально когда несколько индексов изначально. Отвечает за уровень, по которому будет проведена стыковка
2. dropna=True, строки где NaN в каждой колонке - уберет. Если поставить False то все ок.

## .xs()
Чтобы орудовать с уровнями и получать новый таблицы.

Пример 1:
	![[Pasted image 20230808214846.png]]

Пример 2:

	![[Pasted image 20230808214919.png]]


## .unstack()
**Unstack** (расстыковка) — операция, противоположная `` `stack` ``, которая помещает уровень индекса строк в уровень оси столбцов.

Пример:
	![[Pasted image 20230808220405.png]]

Для того, чтобы сделать расстыковку по нескольким уровням, необходимо передать список, состоящий либо только из чисел, либо только из названий. Поместим параметры в столбцы, а имена оставим в индексах.

Пример: 
	![[Pasted image 20230808220537.png]]


## .melt()
С помощью этого метода можно расплавить данные и привести к длинному формату. Так одна или несколько колонок помещаются в качестве идентификационных переменных, а остальные стобцы считаются измеряемыми переменными.

Например:
	![[Pasted image 20230809121646.png]]

	 ![[Pasted image 20230809121719.png]]

## .pd.wide_to_long()
Еще один вариант для перевода данных из широкого формата в длинный.
Пример:
	![[Pasted image 20230809122243.png]]

	![[Pasted image 20230809122329.png]]

## .explode() - раскрывает листы в ячейках
	![[Pasted image 20230809124544.png]]

# Управление временем
## .resample()
Теперь посмотрим, какие возможности pandas предоставляет для работы с временными рядами. Один из наиболее часто используемых и удобных методов —позволяющий преобразовать данные и применить к ним другой метод (`` `sum()` ``, `` `size()` `` и пр.).

	![[Pasted image 20230809125637.png]]

Например, посчитать сумму показателя по дням, имея данные по часам, можно следующим образом:

	data.resample(rule='D').sum()

А если за каждый 6 часов, то:

	data.resample(rule='6H').sum()

В качестве индексов датафрейма обязательно нужно использовать колонку формата `` `DateTime` ``, отсортированную в правильном порядке. Поэтому предварительно всегда следует проверить правильность типа данных и, если требуется, привести его к правильному с помощью `` `pd.to_datetime()` ``.

# Стиль
Помимо методов для работы с данными, `` `pandas` `` включает в себя возможности для форматирования таблиц!

Например: 

- `` `df.style.highlight_null()` `` – подсветить ячейки с пропущенными значениями 
- `` `df.style.highlight_max()` `` – подсветить ячейки с максимальными значениями по колонкам
- `` `df.style.highlight_min()` `` – подсветить ячейки с минимальными значениями по колонкам
- `` `df.style.applymap(func)` `` – применить стилевую функцию к каждой ячейке датафрэйма
- `` `df.style.apply(func, axis, subset)` `` – применить стилевую функцию к каждой колонке/строке в зависимости от `` `axis` ``, `` `subset` `` позволяет выбрать часть колонок для оформления
- `` `render()` `` – после декорирования возвращает HTML, описывающий табличку

## .hide_index()
Позволяет спрятать индексы

## .set_caption() 
С его помощью можно добавить подписи к таблице

## .highlight_max()
Подсвечивает (выделяет) цветом наибольшее значение. Можно применить либо к каждой строке (`` `axis=0/'index'` ``), либо к каждой колонке (`` `axis=1/'columns'` ``).

## .highlight_min()
Противоположна предыдущей
## background_gradient
Раскрашивает ячейки в зависимости от их значений. В итоге получается что-то похожее на heatmap (тепловую карту)

Пример:
	![[Pasted image 20230809150652.png]]

## .style.bar()
Визуализировать значения можно прямо в таблице с помощью `` `.bar()` ``. Данный метод принимает несколько аргументов:

- `` `subset` `` – для каких колонок нужно построить небольшой барплот
- `` `color` `` – цвет
  
		df.style.bar(subset=['C', 'D'], color='#67A5EB')

## Форматирование данных в табличке
	![[Pasted image 20230809151058.png]]
	