## SQL - команды
**Типы команд**
	1. DDL (Data Definition Language)
		- CREATE
		- ALTER
		- DROP
		- RENAME
		- TRUNCATE
	2. DML (Data Manipulation Language)
		SELECT
		INSERT
		UPDATE
		DELETE
	3. TCL (Transaction Control Language)
		COMMIT - применяет транзакцию
		ROLLBACK - откатывает все изменения в контексте текущей траназкйии
		SAVEPOINT - делит транзакцию на более мелкие участки
	4. DCL (Data Control Language)
		GRANT - предоставление прав
		REVOKE - отзыв прав
		DENY - запрет

## Порядок выполнения запроса
1. `FROM` (выбор таблицы)
2. `JOIN` (комбинация с данными из другой таблицы)
3. `WHERE` (фильтрация)
4. `GROUP BY` (агрегирование)
5. `HAVING` (фильтрация агрегированных данных)
6. `SELECT` (возврат результата)
7. `ORDER BY` (сортировка).

## IN и EXISTS, ALL ANY

	![[Pasted image 20240305110840.png]]
	![[Pasted image 20240305110904.png]]
Выбор между `IN` и `EXISTS` зависит от конкретной задачи и структуры данных. В целом, `IN` часто используется для сравнения с фиксированным списком значений, тогда как `EXISTS` полезен, когда нужно проверить наличие строк, удовлетворяющих определенному условию в другой таблице или подзапросе.

**ALL**. Оператор **ALL** возвращает TRUE, если условие выполняется для всех значений, возвращенных подзапросом
		![[Pasted image 20240305145343.png]]
**ANY**. Оператор **ANY** возвращает TRUE, если условие выполняется хотя бы для одного значения возвращенного подзапросом.
		![[Pasted image 20240305145322.png]]

**LIKE and RLIKE and SIMILAR TO and ILIKE and ~**
LIKE не поддерживает REGEXP

## Оптимизация и индексы

Идексация позволяет при помощи созданного индекса найти быстрее данных по запросу.
**Создание индекса**
	![[Pasted image 20240216093233.png]]
В SQL есть систмеа управления реляционными базами данных (СУРБД) состоящая из:
- Оптимизатора запросов - компонент СУРБД, определяющий наиболее эффективный способ выполнения запроса с учетом анализа всех возможных 
	- ![[Pasted image 20240216094923.png]]планов.
	- (Пример, у нас есть индексация по столбцу *category*, и мы пишем запрос
	 ![[Pasted image 20240216093546.png]]
	 Несмотря на то что *category* идет позже, чем *product_subcategory*, оптимизатор выбере путь сначала смотреть на *category*, т.к. у нее есть индекс.
Также можно создать индекс для более чем одного столбца
	![[Pasted image 20240216094059.png]]
Однако важно помнить, что:
	1. Индексам необходимо место для хранения
	2. При добавлении данных в БД сначала обновляется исходная таблица, а затем все ее индексы
Поэтому стоит использовать индексы для БД, которая получает новые данные планово. А для БД, которые обновляются постоянно это будет бесполезно.

**Типы индексов**
1. Кластеризованные индексы - особый индекс, который использует первичный ключ для структуризации данных в таблице. Он не требует явного объявления и создается по умолчанию при определении ключа. Отсортированный же в порядке возрастания первичный ключ по умолчанию применяется в качестве кластеризованного индекса. 
	В этом случае БД использует двоичный поиск (повторяющеес деление данных пополам и определение того, находится ли искомая запись до или после записи в середеине структуры данных)
	Аналогичным образом для нашего датасета с 12 миллионами строк понадобится не 12 миллионов, а всего лишь 24 поиска  —  и всё благодаря двоичному поиску. Думаю, теперь вы осознаете супер силу индексов.
		![[Pasted image 20240216094957.png]]
2. Некластеризованный индекс - это индексы которые требуют явного определения. Некластеризованный индекс хранится в одном месте, а физические данные таблицы в другом. Опять нам на ум приходит сравнение со страницей индексов, которая размещается отдельно от содержимого книги.
	 ![[Pasted image 20240216095123.png]]
	 БД совершает 3 шага: 

	- Во-первых, она переходит по некластеризованному индексу (`product_category_index`) и методом двоичного поиска находит искомую запись столбца (`category = ‘electronics’`).
	- Во-вторых, в основной таблице она ищет адреса соответствующей строки, в которой находится запись столбца. 
	- В-третьих, она переходит к этой строке в основной таблице и выбирает другие значения столбца в соответствии с требованиями запроса (`product_name, price`).

**B-деревья**
	![[Pasted image 20240216095730.png]]
	Поиск в B-дереве очень схож с поиском в бинарном дереве, только здесь мы должны сделать выбор пути к потомку не из 2 вариантов, а из нескольких. В остальном — никаких отличий.

**Хэш-индексы (Hash Indexes)** 
	Идея хеширования состоит в том, чтобы значению любого типа данных сопоставить некоторое небольшое число (от 0 до _N_−1, всего _N_ значений). Такое сопоставление называют _хеш-функцией._
	Полученное число можно использовать как индекс обычного массива, куда и складывать ссылки на строки таблицы (TID). Элементы такого массива называют _корзинами хеш-таблицы_ — в одной корзине могут лежать несколько TID-ов, если одно и то же проиндексированное значение встречается в разных строках.  
	Хеш-функция тем лучше, чем равномернее она распределяет исходные значения по корзинам. Но даже хорошая функция будет иногда давать одинаковый результат для разных входных значений — это называется _коллизией._ Так что в одной корзине могут оказаться TID-ы, соответствующие разным ключам, и поэтому полученные из индекса TID-ы необходимо перепроверять.
		![[Pasted image 20240216100232.png]]
		Хеш-индекс, как видно на картинке, использует страницы (серые прямоугольники) четырех видов:  
		1. Метастраница (meta page) — нулевая страница, содержит информацию о том, что находится внутри индекса;
		2. Страницы корзин (bucket page) — основные страницы индекса, хранят данные в виде пар «хеш-код — TID»;
		3. Страницы переполнения (overflow page) — устроены так же, как страницы корзин, и используются в случае, когда одной страницы для корзины не хватает;
		4. Страницы битовой карты (bitmap page) — в них отмечаются освободившиеся страницы переполнения, которые можно использовать для других корзин.
**Полнотекстовые индексы (Full-Text Indexes)** 
Полнотекстовые индесы предназначены для поиска текстовых данных, таких как статьи, документы или комментарии. Они позволяют выполнить поиск по словам или фразам, а не только по точному совпадению ключевых слов.

**Уникальные индексы (Unique Indexes):** Уникальные индексы гарантируют уникальность значений в индексируемом столбце или наборе столбцов. Они предотвращают дублирование данных и обеспечивают целостность данных.

## Гибридные операции INSERT, UPDATE

	INSERT INTO employees (id, name, salary)
	VALUES (1, 'John Doe', 50000)
	ON DUPLICATE KEY UPDATE salary = 50000;
Вставляем значения, если есть дублю по ключу - то делаем update поля

## MERGE / REPLACE / UPDATE
1. **Merge**
	MERGE INTO target_table USING source_table ON condition
	WHEN MATCHED THEN
	    UPDATE SET column1 = value1, column2 = value2
	WHEN NOT MATCHED THEN
	    INSERT (column1, column2) VALUES (value1, value2);
2. **Replace**
	REPLACE INTO table_name (column1, column2) VALUES (value1, value2);
3. **Insert**
	INTO table_name (column1, column2)
	VALUES (value1, value2)
	ON DUPLICATE KEY UPDATE column1 = value1, column2 = value2;

## Хранимые функции, процедуры и триггеры
**Функции**
		CREATE FUNCTION calculate_tax(amount DECIMAL(10,2)) RETURNS DECIMAL(10,2)
		BEGIN
		    DECLARE tax DECIMAL(10,2);
		    SET tax = amount * 0.1; -- Например, налог в 10%
		    RETURN tax;
		END;

**Процедуры**
CREATE PROCEDURE add_employee(IN emp_name VARCHAR(255), IN emp_salary DECIMAL(10,2))
BEGIN
    INSERT INTO employees (name, salary) VALUES (emp_name, emp_salary);
END;

**Триггеры**
	CREATE TRIGGER before_employee_insert
	BEFORE INSERT ON employees
	FOR EACH ROW
	BEGIN
	    SET NEW.creation_date = NOW(); -- Присвоить текущую дату и время при вставке новой записи
	END;

