 # HTTP REST API

**Способы общения клиента и сервера:
1. HTTP - сетевой протокол прикладного уровня
2. REST API - архитектурный стиль
3. SOAP - протокол обмена структурированными сообщениями
4. GRAPHQL - язык запросов
5. WEBSOCKET - сетевой протокол
6. RPC - удаленный выозов процедур

В клиент серверной архитектуре, сервер выступает источником вычислений и вычислительных мощностей, хранения данных, а клиенты могут с этим сервером взаимодействовать.

HTTP - гипертекстовый транспортный протокол, уровень приложений. С помощью HTTP обращаются большиснвто приложений в интернете.



API - application programming interface - интерфейс, который описывает как общеться с программой. 
**Пример**, есть программа которая умеет делать следующее:
![[Pasted image 20240112185313.png]]
Какие параметры нужно отправить на сервер, чтобы получить прогноз погоды на неделю:
![[Pasted image 20240112185330.png]]

REST API концепция:




## Протокол
Протокол HTTP - изначально был придуман для передачи ГиперТекстовых Документов (HTML), сегодня для передачи произвольных данных.

Протокол имеет 2 главных аспекта: ресурсы и HTTP-глаголы.

### Ресурсы
Это и есть тот самый URL:
![[Pasted image 20240113233605.png]]
А для того чтобы работать с этим ресурсом нам нужны HTTP-глаголы

### HTTP-глаголы
- GET - получить информаци
- PUT - обновить информацию
- DELETE - удалить объект
- POST - добавить объект

### REST
Архитектурный стиль взаимодействия компонентов распределенного приложения в сети. Архитектурный стиль - набор согласованных ограничений и принципов проектирования, позволяющий добиться определенных свойств системы.
Назначение REST-а в том, чтобы дать проектируемой системе такие свойства, как:
- Производительность
- Масштабируемость
- Гибкость к изменениям
- Отказоустойчивость
- Простота поддержки
Это наиболее ценные свойства - нефункциональные требования. Для того, чтобы их добиться есть основные принципы REST-а.

### Принципы REST
1. Клиент-серверная архитектура
	Прнцип подразумевает в себе разделение некоторых зон ответственности: в разделении функций клиента и сервера. Это означет, что на стороне клиента лишь функциональное взаимодействие с сервером. При этом сервер реализует в себе логику хранения данных, сложные взаимодействия со смежными системами и т.д.
	Плюсы - **масштабируемость**, Минусы - если сервер отъебнет, то система перестанет работать.
2.  Stateless
	Принцип заключается в том, что сервер не должен хранить в себе информацию о сессии с  клиентом. 
	Например, Stateful серверу мы можем отправить запрос, какая погода 21.06 в МСК. А на следующий день могли бы просто спросить, какая погода завтра. Так он бы запомнил наш ответ и "завтра" высчитыват бы на основе записанных данных.
	Такой подход удобен, однако в REST - stateless т.к это даст6
	- масташбируемость. Так как каждый запрос содержит в себе весь контент, можно клонировать сервер и посылать запрос на клон. 
	- простота поддержки. В логах будет храниться абсолютно вся информация  о запросе клиента.
3. Кэширование
	Этот принцип говорит, что каждый запрос клиента должен иметь пометку, нужно ли его кжшировать или нет. Зачем? Тот же кейс с погодой. В первый раз сервер обратиться к метеостанции и сохранит инфу в кэше. Во второй раз сервер просто достанет инфу из кэша.
	Плюсы очевидны - уменьшение кол-ва сетевых взаимодействий  и уменьшение нагрузки на системы.
4. Единообразие интерфейса - HATEOAS
	Hypermedia as the Engine of Application State - одно из ограничений REST, согласно которому сервер возвращает не только ресурс, но и его связи с другими ресурсами, и действия, которые можно с ним совершить.
	Получая такие ответы, клиент самостоятельно понимает, какие конкретные действия он может совершать над этим объектом и какую ещё информацию о связанных объектах он может получить. Мы даём клиентскому приложению намного больше информации и свободы действий. Логика клиента становится более гибкой, но при этом и более сложной.
	В качестве обратной стороны медали мы получаем сильное усложнение логики, в первую очередь, клиента. Это может потянуть за собой и усложнение логики на сервере, потому что такие ответы нужно правильно формировать.
5. Layered system (слоистая архитектура)
	Концепция слоистой архитектуры заключается в том, что ни клиент ни сервер не должны знать о том, как происходит цепочка вызовов, дальше своих прямых соседей. Между клиентом и сервером может быть PROXY, Балансировщик.
	Если изменяется поведение proxy-сервера (балансировщика, роутера или чего-то ещё), это не должно повлечь изменения для клиентского приложения или для сервера. Помещая их в эту цепочку вызовов, мы не должны замечать никакой разницы. Это позволяет нам изменять общую архитектуру без доработок на стороне клиента или сервера.
	Минусы: Увеличение нагрузки на сеть т.к. больше участников и больше вызовов,
	Увеличение времени получения ответа
6. Code on done. (Код по требования)
	Например у нас есть браузер в роли клиента. Клиент отправляет запрос и ожидает страницу с интерактивом (например, по клику мыши происходит фейервер). Это все может быть релазиовано на стороне клиента. Клиент запрашивая страницу приветствия, получит не только HTML-код изображения но и код, реализующий фейерверки. Получается сервер передает код, а клиент его запускает.
	Это также дает определенную гибкость, например если хотим поменять цвет фейерверка.

Частые заблуждения:
1. Ограничения REST-опциональны. С точки зрения создателя, только Код по требованию является необязательным. Если одно из остальных не выполняется то это уже не REST подход.
2. REST - протокол передачи данных. REST - это концепция и парадигма. Из протокольского тут только метка о кэшировании данных.


### Модель зрелости REST-сервисов:
	![[Pasted image 20240114002836.png]]

1. **Нулевой уровень:**
К нему относятся любые сервисы которые в качестве транспорта используют HTTP и какойто формат представления данных.
Если более наглядно «пощупать ручками» с точки зрения использования протокола HTTP, то можно представить, что мы выставляем некоторый API. Мы начинаем с того, что объявляем единый путь для отправки команд и всегда используем один и тот же HTTP-глагол для совершения абсолютно любых действий с любыми объектами. Например: создай вебинар, запиши вебинар, удали вебинар и т.д. То есть мы всегда используем один и тот же URL и всегда используем один и тот же HTTP-метод, обычно POST.
	![[Pasted image 20240114003331.png]]
2. **Первый уровень:**
Следующий уровень — **первый**. Мы уже научились использовать разные ресурсы и делаем это не по одному URL. Но при этом всё ещё игнорируем HTTP-глаголы.

Мы просто разделяем явно наши объекты, как некоторые ресурсы. Например: спикер, курс, вебинар. Но, независимо от того, что мы хотим сделать — удалить, создать, редактировать, мы всё равно используем один и тот же HTTP-глагол POST.
	![[Pasted image 20240114003354.png]]

3. **Второй уровень:**
Это когда мы начинаем правильно с точки зрения спецификации HTTP-протокола использовать HTTP-глаголы.
Например, если есть спикер, то, чтобы создать спикера и получить информацию о нём, я использую соответствующий глагол: GET, POST. Когда хочу создать или удалить спикера — я использую глаголы: PUT, DELETE.

По сути, второй уровень зрелости — это то, что чаще всего называют REST.

Надо понимать, что, с точки зрения изначальной концепции, если мы дошли до второго уровня зрелости, то это еще не означает, что мы спроектировали REST-систему/ REST-сервис. Но в очень распространённом понимании соответствие 2-ому уровню часто называют RESTfull сервисом.

RESTfull-сервис — это такой сервис, который спроектирован с учётом REST-ограничений. Хотя, в целом, правильнее сервис такого уровня зрелости называть HTTP-сервисом или HTTP-API, нежели REST-API.
	![[Pasted image 20240114003552.png]]
4. **Третий уровень:**
это уровень, в котором мы начинаем использовать концепцию HATEOAS. Когда мы передаём информацию, ресурсы, мы сообщаем потребителям (клиентам) о том, какие ещё действия необходимо совершить ресурсу, а также связи с другими ресурсами.
	![[Pasted image 20240114003725.png]]
[REST, что же ты такое? Понятное введение в технологию для ИТ-аналитиков / Хабр (habr.com)](https://habr.com/ru/articles/590679/)
Геркин
Vision scope



### Архитектура ПО
Монолиты:
+ Можно быстрь развернуть
+ Плохо масштабируется
+ Одна ошибку нарушит работу всего сервиса
Микросервисы:
- Программа разбивается на отдельные сервисы, каждый из которых выполняет свою функцию
- Более гибкий, масштабируем, изменения одного сервиса не повлияет на работу других
- Нужно доп ресерсу, чтобы согласовывать работу между ними.
